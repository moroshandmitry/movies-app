import styled from 'styled-components/macro';

import { StyledWidthLimiter } from '../../../styles';

export const StyledMainWidthLimiter = styled(StyledWidthLimiter)`
    height: 100%;
    margin: 0 auto;
`;
